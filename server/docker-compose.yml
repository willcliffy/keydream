
version: "3"
services:
  lobby:
    build:
      context: .
      dockerfile: Dockerfile.lobby
    command: CompileDaemon -log-prefix=false -build="make lobby-build" -command="./_dist/lobby"
    volumes:
      - .:/app
    ports:
      - "8080:80"
    networks:
      - backend
  world:
    build:
      context: .
      dockerfile: Dockerfile.world
    command: CompileDaemon -log-prefix=false -build="make world-build" -command="./_dist/world"
    volumes:
      - .:/app
    ports:
      - "8081:80"
    networks:
      - backend
networks:
  backend: